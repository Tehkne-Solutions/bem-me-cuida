import { forwardRef } from 'react';import { StyleSheet,TextInput,View,type TextInputProps } from 'react-native';import { AppText } from '@/components/AppText';import { colors,radius,spacing } from '@/theme/tokens';type Props=TextInputProps&{label:string;error?:string|null|undefined;hint?:string};export const TextField=forwardRef<TextInput,Props>(function TextField({label,error,hint,style,...props},ref){return <View style={styles.group}><AppText variant="bodyStrong">{label}</AppText><TextInput ref={ref} {...props} style={[styles.input,error&&styles.errorInput,style]} placeholderTextColor={colors.textMuted} accessibilityLabel={props.accessibilityLabel??label} accessibilityState={{disabled:props.editable===false}}/>{error?<AppText variant="caption" style={styles.errorText}>{error}</AppText>:null}{!error&&hint?<AppText variant="caption" muted>{hint}</AppText>:null}</View>});const styles=StyleSheet.create({group:{gap:spacing.sm},input:{minHeight:52,borderWidth:1,borderColor:colors.border,borderRadius:radius.md,backgroundColor:colors.surface,color:colors.text,fontSize:16,paddingHorizontal:spacing.lg,paddingVertical:spacing.md},errorInput:{borderColor:colors.danger},errorText:{color:colors.danger}});
